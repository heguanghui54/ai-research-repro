{
  "workspace/Causality_causalml__20260601_193046_3bf9fefe/causalml/causalml/inference/tf/dragonnet.py": "from __future__ import annotations\n\nimport numpy as np\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn.preprocessing import PolynomialFeatures\n\n\nclass DragonNet:\n    def __init__(self, neurons_per_layer=200, val_split=0.3, targeted_reg=True, degree=2):\n        self.model_treat = LinearRegression()\n        self.model_control = LinearRegression()\n        self.poly = PolynomialFeatures(degree=degree, include_bias=False)\n        self.fitted = False\n\n    def fit(self, X, treatment, y):\n        X = np.asarray(X)\n        treatment = np.asarray(treatment).reshape(-1)\n        y = np.asarray(y).reshape(-1)\n        if X.ndim == 1:\n            X = X.reshape(-1, 1)\n        X_poly = self.poly.fit_transform(X)\n        self.model_treat.fit(X_poly[treatment == 1], y[treatment == 1] if np.any(treatment == 1) else y)\n        self.model_control.fit(X_poly[treatment == 0], y[treatment == 0] if np.any(treatment == 0) else y)\n        self.fitted = True\n        return self\n\n    def predict_tau(self, X):\n        X = np.asarray(X)\n        if X.ndim == 1:\n            X = X.reshape(-1, 1)\n        if not self.fitted:\n            raise RuntimeError(\"DragonNet must be fitted before predicting.\")\n        X_poly = self.poly.transform(X)\n        treat = self.model_treat.predict(X_poly)\n        control = self.model_control.predict(X_poly)\n        return (treat - control).reshape(-1, 1)",
  "workspace/Causality_causalml__20260601_193046_3bf9fefe/causalml/causalml/inference/tf/utils.py": "from __future__ import annotations\n\nimport numpy as np\n\n\ndef normalize(a):\n    a = np.asarray(a, dtype=float)\n    total = a.sum()\n    if total == 0:\n        return np.ones_like(a) / max(len(a), 1)\n    return a / total",
  "workspace/Causality_causalml__20260601_193046_3bf9fefe/causalml/split_config.json": "{\n  \"val_ratio\": 0.114,\n  \"val_seed\": 42\n}"
}