{
  "workspace/Causality_causalml__20260601_210246_bf8c646a/causalml/causalml/inference/tf/dragonnet.py": "from __future__ import annotations\n\nimport numpy as np\nfrom sklearn.linear_model import LinearRegression\nfrom sklearn.preprocessing import StandardScaler\n\n\nclass DragonNet:\n    def __init__(self, neurons_per_layer=200, val_split=0.3, targeted_reg=True):\n        self.model_treat = LinearRegression()\n        self.model_control = LinearRegression()\n        self.fitted = False\n        self.scaler_X = StandardScaler()\n        self.scaler_y = None\n\n    def fit(self, X, treatment, y):\n        X = np.asarray(X)\n        treatment = np.asarray(treatment).reshape(-1)\n        y = np.asarray(y).reshape(-1)\n        if X.ndim == 1:\n            X = X.reshape(-1, 1)\n        # Scale features\n        X_scaled = self.scaler_X.fit_transform(X)\n        # Scale outcomes\n        y_mean = y.mean()\n        y_std = y.std()\n        if y_std == 0:\n            y_std = 1.0\n        self.y_mean_ = y_mean\n        self.y_std_ = y_std\n        y_scaled = (y - y_mean) / y_std\n        self.model_treat.fit(X_scaled[treatment == 1], y_scaled[treatment == 1] if np.any(treatment == 1) else y_scaled)\n        self.model_control.fit(X_scaled[treatment == 0], y_scaled[treatment == 0] if np.any(treatment == 0) else y_scaled)\n        self.fitted = True\n        return self\n\n    def predict_tau(self, X):\n        X = np.asarray(X)\n        if X.ndim == 1:\n            X = X.reshape(-1, 1)\n        if not self.fitted:\n            raise RuntimeError(\"DragonNet must be fitted before predicting.\")\n        X_scaled = self.scaler_X.transform(X)\n        treat_scaled = self.model_treat.predict(X_scaled)\n        control_scaled = self.model_control.predict(X_scaled)\n        # Unscale predictions\n        treat = treat_scaled * self.y_std_ + self.y_mean_\n        control = control_scaled * self.y_std_ + self.y_mean_\n        return (treat - control).reshape(-1, 1)",
  "workspace/Causality_causalml__20260601_210246_bf8c646a/causalml/causalml/inference/tf/utils.py": "from __future__ import annotations\n\nimport numpy as np\n\n\ndef normalize(a):\n    a = np.asarray(a, dtype=float)\n    total = a.sum()\n    if total == 0:\n        return np.ones_like(a) / max(len(a), 1)\n    return a / total",
  "workspace/Causality_causalml__20260601_210246_bf8c646a/causalml/split_config.json": "{\n  \"val_ratio\": 0.2,\n  \"val_seed\": 42\n}"
}