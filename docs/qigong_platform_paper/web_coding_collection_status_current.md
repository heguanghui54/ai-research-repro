# 网页人工编码采集当前状态

更新时间：2026-06-01

## 已完成

- 已建立健身气功短视频网页人工编码平台。
- 已接入 Supabase 数据库，包含视频任务表与学生提交表。
- 已将 120 条正式主编码样本导入数据库。
- 已设置 `STU01` 至 `STU10` 的学生分工。
- 已为 `STU04` 与 `STU09` 各加入 3 条复核任务，用于后续一致性检验。
- 已在学生端加入中文填报说明、视频打开按钮、观看确认、正式编码变量选择、证据标记和备注字段。
- 已将网页字段扩展到正式编码表所需的身体经验变量、平台踪迹变量与延异变量，并建立网页导出转正式编码表脚本。
- 已设置无视频链接条目为暂缓编码，避免学生仅凭标题或 LLM 建议硬填。
- 已验证匿名 REST 不能直接读取 `coding_videos` 与 `coding_submissions` 表。
- 已将教师导出管理口令从代码中移除，改为本机私有环境变量调用。

## 当前导出审计

来自 `runs/qigong_platform/formal_merge/web_coding_submissions_export_summary.json`：

| 指标 | 当前值 |
|---|---:|
| 预期主编码任务 | 120 |
| 已提交总行数 | 0 |
| 完整提交行数 | 0 |
| 主编码提交 | 0 |
| 复核编码提交 | 0 |
| 主编码剩余下限 | 120 |

网页导出转换管道已通过空提交状态 smoke 测试：`runs/qigong_platform/formal_merge/qigong_human_coding_from_web.csv` 能生成 120 行正式编码表结构；由于当前提交数为 0，完成度审计为 warn，不可解释为正式结果。

## 论文中目前可以写

- 可以写“本研究建立了多人协同网页人工编码平台，并启动 120 条正式样本的人工编码采集”。
- 可以写平台采集法的技术路线：Vercel 前端/API、Supabase 存储、学生编号分配、观看确认、编码变量、提交时间、导出审计。
- 可以写数据纪律：无视频链接条目暂缓，LLM 辅助建议不替代人工编码，正式结果必须通过完整性审计与复核一致性检验。

## 论文中目前不能写

- 不能写“人工编码结果显示……”。
- 不能报告各变量占比、平台差异、卡方检验、聚类、回归或相关分析。
- 不能把 deepseek-chat 预填结果当作人工编码结果。
- 不能把 0 提交状态包装成正式经验发现。

## 下一步

1. 让学生按 `STU01` 至 `STU10` 登录网页完成有视频链接的任务。
2. 补齐缺失视频链接，尤其是抖音、快手、小红书样本。
3. 导出学生提交，审计完整性与异常值。
4. 运行 `scripts/ingest_qigong_web_coding_submissions.py` 将网页提交转换为正式编码表。
5. 运行 `scripts/audit_qigong_coding_values.py --profile formal` 检查取值、完成度和人工编码员标记。
6. 计算复核任务的一致性，决定是否需要返工。
7. 将通过审计的人工编码结果合并进正式结果表，再更新论文第 4、6、8 节。

## 与视频证据层的关系

网页人工编码负责 120 条样本的传播语义、身体可见性和延异变量；SportsLabKit/典型视频层只负责少量本地授权视频的身体证据补强。两者不能互相替代：学生网页编码可以先行收集，典型视频分析必须等待 `data/private/qigong_typical_videos/` 中至少 3 个本地视频通过权利与文件预检。

## 投稿总门槛中的位置

`scripts/build_qigong_current_submission_gate.py` 已将网页人工编码列为 P0 门槛。当前状态为 `complete_rows=0`、`primary_submissions=0`，因此论文不能写人工编码结果。学生完成后，应先导出 Supabase 提交，再运行网页提交转正式编码表脚本和 formal 编码审计。
